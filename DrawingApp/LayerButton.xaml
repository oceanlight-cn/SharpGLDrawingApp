<UserControl x:Class="DrawingApp.LayerButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DrawingApp"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" x:Name="toggleButton" Content="隐藏" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="ToggleButton_Click"/>

        <Label Grid.Column="1" x:Name="contentLabel" Content="" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Button Grid.Column="2" x:Name="removeButton" Content="X" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="removeButton_Click"/>
    </Grid>
</UserControl>
